export const designTokens = {
    borderRadii: {
        sm: 10, // Small elements (icon buttons)
        md: 20, // Medium elements (text fields, search bars, cards)
        lg: 25, // Large elements (buttons, cards)
        xl: 29, // Extra large elements (tab bars, rounded UI elements)
        full: 120, // Fully rounded (tab bars, special cases)
    },
    layout: {
        containerPadding: 16, // Default padding for screens
        sectionSpacing: 24, // Space between major sections
        buttonPadding: 12, // Default button padding
        cardPadding: 16, // Default card padding
        inputHeight: 53, // Standard input field height
        buttonHeight: 59, // Standard button height
    },
    navigation: {
        bottomNavPadding: 12, // Padding around bottom navigation icons
        headerPaddingTop: 16, // Space at the top of screens
    },
}
