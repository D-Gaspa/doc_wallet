export const spacing = {
    spacing: {
        xs: 4, // Extra small spacing (tight spaces, small paddings)
        sm: 8, // Small spacing (buttons, inputs)
        md: 16, // Medium spacing (text separation, moderate gaps)
        lg: 24, // Large spacing (cards, containers)
        xl: 32, // Extra large spacing (section spacing, major gaps)
    },
}
